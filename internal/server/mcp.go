package server

import "net/http"

// registerMCP wires the MCP-over-HTTP endpoint. (Task 9.5)
func registerMCP(mux *http.ServeMux, d *Deps) {
	// Placeholder — MCP routes are added in Task 9.5.
	_ = mux
	_ = d
}
