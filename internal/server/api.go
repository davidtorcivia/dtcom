package server

import "net/http"

// registerAPI wires the bearer-token-authenticated REST API. (Task 9.3)
func registerAPI(mux *http.ServeMux, d *Deps) {
	// Placeholder — REST routes are added in Task 9.3.
	_ = mux
	_ = d
}
